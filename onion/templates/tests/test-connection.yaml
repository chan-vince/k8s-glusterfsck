apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onion.fullname" . }}-test-connection"
  labels:
    {{- include "onion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "onion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
