apiVersion: v1
kind: Service
metadata:
  name: {{ include "onion.fullname" . }}
  labels:
    {{- include "onion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "onion.selectorLabels" . | nindent 4 }}
